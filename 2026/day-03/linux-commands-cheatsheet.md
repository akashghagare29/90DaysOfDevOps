# 🐧 Day 03 – Linux Commands Practice

A quick-reference cheat sheet of **commonly used Linux commands** for daily DevOps work.

---

## 📂 File & Directory Management

- `ls` – List files and directories  
- `cd` – Change directory  
- `pwd` – Show current directory path  
- `mkdir` – Create a new directory  
- `rmdir` – Delete an empty directory  
- `cp` – Copy files or directories  
- `mv` – Move or rename files  
- `rm` – Delete files or directories  

---

## 📄 File Viewing & Editing

- `cat` – Display file contents  
- `less` – View large files page by page  
- `head` – Show first lines of a file  
- `tail` – Show last lines of a file  
- `touch` – Create an empty file  

---

## 🔐 Permissions & Ownership

- `chmod` – Change file permissions  
- `chown` – Change file owner/group  
- `ls -l` – View permissions in detail  

---

## 🧠 Process & System Monitoring

- `ps aux` – List running processes  
- `top` – Real-time process monitoring  
- `kill` – Terminate a process  
- `uptime` – Check system running time  

---

## 🌐 Networking Commands

- `ping` – Check network connectivity  
- `ip addr` – View IP addresses  
- `curl` – Test APIs or URLs from terminal  
- `traceroute` – Show network path to a host
- `nslookup` – Get DNS resolution details

---

## 💽 Disk & Memory Usage

- `df -h` – Check disk usage  
- `du -sh` – Check directory size  
- `free -m` – Check memor
